import React from 'react';
import styles from '../css/Header.module.css';

let Header = () => {
  return (
    <header>
      <h1>My Movie App</h1>
      <button>Menu</button>
    </header>
  );
};

export default Header;
